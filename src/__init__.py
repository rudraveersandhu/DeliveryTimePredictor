"""Delivery Time Predictor — source package.

Modules
-------
features : cleaning the raw Kaggle CSV + the feature-engineering transformer
pipeline : scikit-learn Pipeline builders (preprocessing + XGBoost model)
train    : the training script (loads data, trains, evaluates, saves models)
"""
